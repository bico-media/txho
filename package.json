{
	"name": "txho",
	"version": "2.0.0",
	"description": "Get a Bitcoin transaction as JSON for CLI and node",
	"main": "dist/index.js",
	"bin": {
		"txho": "./dist/cli.js"
	},
	"author": "MWulff",
	"license": "Open-BSV",
	"repository": {
		"type": "git",
		"url": "https://github.com/bicomedia/txho.git"
	},
	"scripts": {
		"go": "ts-node src/index.ts",
		"test": "echo Tests are not automated yet",
		"build": "tsc src/index.ts src/cli.ts  --outDir dist/",
		"prepublishOnly": "yarn build && yarn bump",
		"bump": "yarn is-git-clean && bump --tag 'v%s'",
		"is-git-clean": "(git diff --quiet --exit-code --cached && git diff --quiet --exit-code) || (echo Please commit or stash changes && exit 1)"
	},
	"dependencies": {
		"bitcoind-rpc": "0.8.1",
		"bsv": "1.2.0",
		"dotenv": "8.2.0",
		"global": "^4.4.0",
		"json5": "2.1.3",
		"txo": "0.0.21"
	},
	"devDependencies": {
		"@types/node": "14.0.5",
		"ts-node": "8.10.1",
		"typescript": "3.9.3",
		"version-bump-prompt": "6.0.3"
	},
	"keywords": [
		"txo",
		"txid",
		"bitfs",
		"bitwork",
		"BSV",
		"bitcoin sv",
		"bitcoin"
	],
	"prettier": {
		"useTabs": true,
		"printWidth": 100,
		"singleQuote": true,
		"bracketSpacing": false
	}
}
